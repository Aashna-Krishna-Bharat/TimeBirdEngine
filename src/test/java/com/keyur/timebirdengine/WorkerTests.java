package com.keyur.timebirdengine;

import com.keyur.timebirdengine.dto.BloodGroupCountResponseEntity;
import com.keyur.timebirdengine.entity.type.BloodGroupType;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.data.domain.Page;
import org.springframework.data.domain.PageRequest;
import org.springframework.data.domain.Pageable;
import org.springframework.data.domain.Sort;

import java.time.LocalDate;
import java.util.List;

@SpringBootTest
public class WorkerTests {

	/*
	 * @Autowired private WorkerRepository WorkerRepository;
	 * 
	 * @Autowired private WorkerService WorkerService;
	 * 
	 * @Test public void testWorkerRepository() {
	 * 
	 * List<Worker> WorkerList =
	 * WorkerRepository.findAllWorkerWithAppointment();
	 * System.out.println(WorkerList);
	 * 
	 * }
	 * 
	 * @Test public void testTransactionMethods() { // Worker Worker =
	 * WorkerService.getWorkerById(1L);
	 * 
	 * // Worker Worker = WorkerRepository.findById(1L).orElseThrow(() -> new
	 * EntityNotFoundException("Worker not " + // "found with id: 1"));
	 * 
	 * // Worker Worker = WorkerRepository.findByName("Diya Patel");
	 * 
	 * // List<Worker> WorkerList =
	 * WorkerRepository.findByBirthDateOrEmail(LocalDate.of(1988, 3, 15), "diya" +
	 * // ".patel@example.com");
	 * 
	 * // List<Worker> WorkerList =
	 * WorkerRepository.findByBornAfterDate(LocalDate.of(1993, 3, 14));
	 * 
	 * Page<Worker> WorkerList =
	 * WorkerRepository.findAllWorkers(PageRequest.of(1, 2, Sort.by("name")));
	 * 
	 * for(Worker Worker: WorkerList) { System.out.println(Worker); } // //
	 * List<Object[]> bloodGroupList = WorkerRepository.countEachBloodGroupType();
	 * // for(Object[] objects: bloodGroupList) { // System.out.println(objects[0]
	 * +" "+ objects[1]); // }
	 * 
	 * // int rowsUpdated = WorkerRepository.updateNameWithId("Arav Sharma", 1L);
	 * // System.out.println(rowsUpdated);
	 * 
	 * // List<BloodGroupCountResponseEntity> bloodGroupList =
	 * WorkerRepository.countEachBloodGroupType(); //
	 * for(BloodGroupCountResponseEntity bloodGroupCountResponse: bloodGroupList) {
	 * // System.out.println(bloodGroupCountResponse); // } }
	 */
}
























