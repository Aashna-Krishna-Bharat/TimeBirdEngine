package com.keyur.timebirdengine;

import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
public class InsuranceTests {


	/*
	 * @Test public void testInsurance() { Insurance insurance = Insurance.builder()
	 * .policyNumber("HDFC_1234") .provider("HDFC") .validUntil(LocalDate.of(2030,
	 * 12, 12)) .build();
	 * 
	 * Worker Worker = insuranceService.assignInsuranceToWorker(insurance, 1L);
	 * 
	 * System.out.println(Worker);
	 * 
	 * var newWorker =
	 * insuranceService.disaccociateInsuranceFromWorker(Worker.getId());
	 * 
	 * System.out.println(newWorker); }
	 * 
	 * 
	 * @Test public void testCreateAppointment() { Appointment appointment =
	 * Appointment.builder() .appointmentTime(LocalDateTime.of(2025, 11, 1, 14, 0,
	 * 0)) .reason("Cancer") .build();
	 * 
	 * // var newAppointment = appointmentService.createNewAppointment(appointment,
	 * 1L, 2L);
	 * 
	 * // System.out.println(newAppointment);
	 * 
	 * // var updatedAppointment =
	 * appointmentService.reAssignAppointmentToAnotherDoctor(newAppointment.getId(),
	 * 3L);
	 * 
	 * // System.out.println(updatedAppointment); }
	 */
}











